import { Meteor } from 'meteor/meteor';

// const Test = new Mongo.Collection('tests');
// se imporatara desde import para terner mas orden

import Items from '../import/api/items';


Meteor.startup(() => {

  });
  // code to run on server at startup
